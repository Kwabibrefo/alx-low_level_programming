#include <stdio.h>

/**
 * main - entry point
 * Description: code to print letters in base 10
 * return: return 0 (success)
 */

int main(void)
{
	for (int a = 0; a <= '10'; a++)
		putchar (a);
	return (0)
}
