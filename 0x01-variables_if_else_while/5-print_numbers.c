#include <stdio.h>

/**
 * main - entry point
 * Description: code to print letters in base 10
 * return: return 0 (success)
 */

int main(void)
{
	int a;

	for (a = 0; a <= 10; a++)
		putchar(a);
	putchar('\n');
	return (0);
}
