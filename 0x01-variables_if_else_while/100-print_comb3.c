#include <stdio.h>

/**
 * main - entry poit
 * Description: print combs
 * Return: return 0 (succcess)
 */

int main(void)
{
	int i = 0;
	int j = 0;

	while (i < 9 && j < 9)
	{_putchar(i);
		_putchar(j);
		_putchar(' ');
		_puchar(',');
	}
}
