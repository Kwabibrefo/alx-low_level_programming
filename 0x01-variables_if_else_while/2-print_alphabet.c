#include<stdio.h>
#include<stdlib.h>

/**
 * main - Entry point
 * Description: 'print using print f'
 * Return: Always 0 (Success)
 */

int main(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; letter++)
		putchar(letter);
	return (0);
}
