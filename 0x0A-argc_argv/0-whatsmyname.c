#include <stdio.h>

/**
 * maint - entry
 * @argc: argv array size
 * @argv: arg array
 * Return: return zero (success)
 */

int main(int argc, char **argv)
{
	(void) argc;

	printf("%s\n", argv[0]);
	return (0);
}
