#include <stdio.h>

/**
 * maint - entry
 * Description: prog to print file name
 * Return: return 0 (success)
 */

int main (int argc, char **argv)
{
	(void) argc;
		printf ("%s\n",argv[0]);
	return (0);
}
