#!/usr/bin/python3
# test_mylib.py
import ctypes

# Load the dynamic library
mylib = ctypes.CDLL('./mylib.so')

# Call the C function from the library
num = 5
result = mylib.factorial(num)
print(f"The factorial of {num} is {result}")
