#include "main.h"

/**
 * _islower - entry point
 * Description : predict let cases
 * Return: return 0,1 (success)
 */

int _islower(int c)
{
	if (c >= 97; c < 122)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}

