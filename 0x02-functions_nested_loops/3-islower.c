#include "main.h"

/**
 * _islower - entry point
 * Description : predict let cases
 * Return: return 0,1 (success)
 */

int _islower(int c)
{
	int i = 65;

	if (i >= 97; i < 122)
	{
		_putchar(1);
	return (1);

	}
	return (0);
}
